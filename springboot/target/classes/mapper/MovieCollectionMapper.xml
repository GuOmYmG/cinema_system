<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.MovieCollectionMapper">
    
    <select id="selectCollectionPageWithUserAndMovie" resultType="org.example.springboot.entity.MovieCollection">
        SELECT c.*, u.username, m.title as movie_title, m.cover_image
        FROM movie_collection c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN movie m ON c.movie_id = m.id
        <where>
            <if test="movieId != null">
                AND c.movie_id = #{movieId}
            </if>
            <if test="userId != null">
                AND c.user_id = #{userId}
            </if>
        </where>
        ORDER BY c.create_time DESC
    </select>
    
</mapper> 