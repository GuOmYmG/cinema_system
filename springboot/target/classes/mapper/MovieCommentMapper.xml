<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.MovieCommentMapper">
    
    <select id="selectCommentPageWithUserAndMovie" resultType="org.example.springboot.entity.MovieComment">
        SELECT c.*, u.username, m.title as movie_title
        FROM movie_comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN movie m ON c.movie_id = m.id
        <where>
            <if test="movieId != null">
                AND c.movie_id = #{movieId}
            </if>
            <if test="userId != null">
                AND c.user_id = #{userId}
            </if>
            <if test="status != null">
                AND c.status = #{status}
            </if>
        </where>
        ORDER BY c.create_time DESC
    </select>
    
</mapper> 