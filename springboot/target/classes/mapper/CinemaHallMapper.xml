<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springboot.mapper.CinemaHallMapper">
    
    <select id="selectHallPageWithCinema" resultType="org.example.springboot.entity.CinemaHall">
        SELECT h.*, c.name as cinema_name
        FROM cinema_hall h
        LEFT JOIN cinema c ON h.cinema_id = c.id
        <where>
            <if test="cinemaId != null">
                AND h.cinema_id = #{cinemaId}
            </if>
            <if test="name != null and name != ''">
                AND h.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="status != null">
                AND h.status = #{status}
            </if>
        </where>
        ORDER BY h.create_time DESC
    </select>
    
    <select id="selectHallsByCinema" resultType="org.example.springboot.entity.CinemaHall">
        SELECT h.*, c.name as cinema_name
        FROM cinema_hall h
        LEFT JOIN cinema c ON h.cinema_id = c.id
        WHERE h.cinema_id = #{cinemaId}
        ORDER BY h.name ASC
    </select>
    
</mapper> 